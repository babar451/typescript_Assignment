const visit = ["Makkah", "Cairo", "NYC", "Maldives", "Santorini"];
// Printing array in original form
console.log(visit);
// Printing array in alphabetic order
let sorted = console.log(visit.slice().sort());
// Showing that the array is still in its original form
console.log(visit);
//Printing array in reverse alphabetic order
let reverse = visit.slice().sort();
console.log(reverse.reverse());
// Showing that the array is still in its original form
console.log(visit);
// reversing the order of the list
console.log(visit.reverse());
// Again reversing the order of the list to make it in its original form
console.log(visit.reverse());
// Sort your array so it’s stored in alphabetical order. Print the array to show that its order has been changed.
console.log(visit.sort());
// Sort to change your array so it’s stored in reverse alphabetical order. Print the list to show that its order has changed.
console.log(visit.reverse());